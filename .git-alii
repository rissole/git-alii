# just source this bad boy and it's ready to go
alias ga="git add"
alias gc="git commit"
alias gd="git diff"
alias gco="git checkout"
alias gp="git push"
alias gl="git pull"
alias gau="git add -u"
alias gst="git status"
alias gph="git push origin HEAD"
alias gfu="gc --amend --no-edit"
gfco () {
  git fetch origin $1; git checkout $1
}
